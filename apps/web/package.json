{
  "name": "@nephix/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "set -a; [ -f ../../.env ] && . ../../.env; set +a; next dev",
    "build": "set -a; [ -f ../../.env ] && . ../../.env; set +a; next build",
    "start": "set -a; [ -f ../../.env ] && . ../../.env; set +a; next start",
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@nephix/contracts": "workspace:*",
    "@nephix/db": "workspace:*",
    "@nephix/domain": "workspace:*",
    "@nephix/ui": "workspace:*",
    "argon2": "0.41.1",
    "jose": "5.9.6",
    "next": "15.1.9",
    "react": "19.0.1",
    "react-dom": "19.0.1",
    "zod": "3.24.1"
  },
  "devDependencies": {
    "@types/node": "22.12.0",
    "@types/react": "19.0.2",
    "@types/react-dom": "19.0.2",
    "typescript": "5.7.2"
  }
}
